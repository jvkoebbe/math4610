# Homework Questions #2.

**Question 1:** Write a pair of codes with a test program that will determine the following about your computer.

**a.** The code should need no input. The code should output the machine precision value in single precision. On many computers, the type for such a data type  would be **float**.

**b.** The code should need no input. The code should output the machine precision value in double precision.On many computers, the type for such a data type  would be **double**.

> **As an aside:**
> 
> In fortran and other such languages the types were **real** or **real*4** and
> **real*8**

**Question 2:** Write three separate functions/routines that do the following:

**a.** Take as input a vector (list or array in programming languages) and return the length of the vector in the $$l_2$$, the Euclidean distance or norm.

**b.** Take as input a vector (list or array in programming languages) and return the length of the vector in the $$l_1$$ or one-norm.

**c.** Take as input a vector (list or array in programming languages) and return the length of the vector in the $$l_\infty$$, the infinity norm or sup-norm.

**Question 3:** Write three separate functions/routines that do the following:

**a.** Take as input two vectors (lists or arrays in programming languages) and return the distance between the vectors in the $$l_2$$, the Euclidean distance or error.

**b.** Take as input two vectors (list or array in programming languages) and return the distance between the vectors in the $$l_1$$ norm.

**c.** Take as input two vectors (list or array in programming languages) and return the distance between the vectors in the $$l_\infty$$, the infinity norm.

**Question 4:** Summarize the work in the previous 3 questions: Create a folder for the safe storage of the 8 routines/functions  you have written. Under some folder or directory in a computer you have ready access to. Create subfolders under the main folder given by

<pre>
/bin /data /doc /inc /lib /src
</pre>

Once you have this structure in place, put your text files for the source code you have written for the assignment into the **/src** folder and await further instruction.
You should also consider include me file. If this is to work well with Markdown, you should name the last file

<pre>
README.md
</pre>

Note that all of the work in this module will appear in another question or two in the next couple of weeks.